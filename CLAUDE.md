# CLAUDE.md

All project instructions live in AGENTS.md (shared by every AI tool). Edit that file, not this one.

@AGENTS.md

## Claude Code specifics

- Before finishing any change, run `npm run lint` and `npm run build`. Both must pass.
- To verify UI changes visually, run `npm run dev` and check both dark and light themes (toggle in the top-right).
